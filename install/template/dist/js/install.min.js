(function(global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports =  factory(require('jquery'), require('@carry0987/utils-full'), require('sweetalert2')) :
  typeof define === 'function' && define.amd ? define(['jquery', '@carry0987/utils-full', 'sweetalert2'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, (global.InstallHelper = factory(global.jQuery,global.Utils,global.Swal)));
})(this, function(jquery, _carry0987_utils_full, sweetalert2) {
//#region \0rolldown/runtime.js
	var __create = Object.create;
	var __defProp = Object.defineProperty;
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __getProtoOf = Object.getPrototypeOf;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") {
			for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
				key = keys[i];
				if (!__hasOwnProp.call(to, key) && key !== except) {
					__defProp(to, key, {
						get: ((k) => from[k]).bind(null, key),
						enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
					});
				}
			}
		}
		return to;
	};
	var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule || !__hasOwnProp.call(mod, "default") ? __defProp(target, "default", {
		value: mod,
		enumerable: true
	}) : target, mod));

//#endregion
jquery = __toESM(jquery, 1);
_carry0987_utils_full = __toESM(_carry0987_utils_full, 1);
sweetalert2 = __toESM(sweetalert2, 1);

//#region template/dist/ts/install.ts
	if (!_carry0987_utils_full.default) throw new Error("Utils not found");
	if (!jquery.default) throw new Error("jQuery not found");
	var InstallHelper = class InstallHelper {
		inputActions = {};
		static backURL = "../public/";
		init() {
			this.checkInstalled();
			this.initValidation();
		}
		showMsg = (isValid, msg, target = "#display") => {
			const message = (isValid ? `<span style='color: green'>` : `<span style='color: red'>`) + msg + `</span>`;
			(0, jquery.default)(target).html(message);
		};
		checkInput = ($element, value) => {
			if (!$element.val()?.toString().trim()) {
				this.showMsg(false, value);
				return false;
			}
			this.showMsg(true, "");
			return true;
		};
		checkPasswordLength = ($element, value) => {
			const password = $element.val()?.toString().trim();
			if (!password || password.length < 8) {
				this.showMsg(false, value);
				return false;
			}
			this.showMsg(true, "");
			return true;
		};
		checkPasswordConfirmation = ($password, $confirmPassword, message) => {
			if ($password.val() !== $confirmPassword.val()) {
				this.showMsg(false, message);
				return false;
			}
			this.showMsg(true, "");
			return true;
		};
		validateInputs = () => {
			const $inputs = (0, jquery.default)("#install input").not("[type=\"submit\"]");
			const isDisplayEmpty = (0, jquery.default)("#display > span").is(":empty");
			const isEmpty = $inputs.toArray().some((input) => {
				return !input.value.trim().length;
			});
			$inputs.each((_index, element) => {
				const inputElement = element;
				const $input = (0, jquery.default)(inputElement);
				if (!inputElement.value.trim().length) $input.addClass("bg-danger-subtle");
				else $input.removeClass("bg-danger-subtle");
			});
			(0, jquery.default)("#submit").prop("disabled", isEmpty || !isDisplayEmpty);
		};
		async sendFormData(url, data, method = "POST", success, errorCallback) {
			return _carry0987_utils_full.default.sendFormData({
				url,
				method,
				data,
				success,
				error: errorCallback
			});
		}
		fetchData = (data, resolveData, method = "POST", parameter = null) => {
			let url = "api.php";
			if (parameter) url += "?" + parameter;
			return new Promise((resolve, reject) => {
				_carry0987_utils_full.default.doFetch({
					url,
					method,
					body: _carry0987_utils_full.default.encodeFormData(data),
					success: function(res) {
						resolve(resolveData(res));
					},
					error: function(error) {
						reject(error);
					}
				});
			});
		};
		async showSwal(popupOptions) {
			const { title, text, html, beforeConfirm, callback, showLoading } = popupOptions;
			delete popupOptions.callback;
			let swal_config = {
				title: title?.toString() ?? "",
				text: text?.toString() ?? "",
				html: html ?? void 0,
				focusConfirm: false,
				showCancelButton: false,
				showCloseButton: false,
				showDenyButton: false,
				showConfirmButton: false,
				allowOutsideClick: !showLoading,
				allowEscapeKey: !showLoading
			};
			swal_config = _carry0987_utils_full.default.deepMerge({}, swal_config, popupOptions);
			if (showLoading && !swal_config.didOpen) swal_config.didOpen = () => {
				sweetalert2.default.showLoading();
			};
			return await sweetalert2.default.fire(swal_config).then((result) => {
				if (beforeConfirm) beforeConfirm(result);
				if (result.isConfirmed || result.isDismissed) {
					if (callback) callback(result);
				}
				return result;
			});
		}
		async langList() {
			return this.fetchData({ request: "get_language" }, (data) => data["lang"]);
		}
		async checkInstalled() {
			const formData = new FormData();
			const lang = await this.langList();
			await this.sendFormData("api.php", {
				request: "check_installed",
				data: formData
			}, "POST", async (res) => {
				sweetalert2.default.hideLoading();
				if (res === true) {
					(0, jquery.default)("#install, #form-title").hide();
					await this.showSwal({
						icon: "error",
						text: lang["install"]["installed"],
						showConfirmButton: true
					}).then((result) => {
						if (result.isConfirmed) window.location.href = InstallHelper.backURL;
					});
				}
			}, async (error) => {
				await this.showSwal({
					title: "Error",
					text: error,
					icon: "error"
				});
			});
		}
		buildInputActions() {
			this.inputActions = {
				"admin": {
					method: this.checkInput,
					messageKey: "username_empty"
				},
				"admin_psw": {
					method: this.checkPasswordLength,
					messageKey: "password_rule"
				},
				"admin_psw_confirm": {
					method: this.checkPasswordConfirmation,
					messageKey: "repassword_error"
				}
			};
		}
		async initValidation() {
			const lang = await this.langList();
			this.buildInputActions();
			(0, jquery.default)("#install").on("input blur propertychange", "input", (e) => {
				const id = e.target.id;
				const action = this.inputActions[id];
				if (action && typeof action.method === "function") {
					const index = action.messageIdx ? action.messageIdx : "install";
					if (id === "admin_psw_confirm") action.method((0, jquery.default)("#admin_psw"), (0, jquery.default)(e.target), lang[index][action.messageKey]);
					else action.method((0, jquery.default)(e.target), lang[index][action.messageKey]);
				}
			});
			(0, jquery.default)("#install").on("input", "input", (event) => {
				(0, jquery.default)(event.target).removeClass("bg-danger-subtle");
			});
			(0, jquery.default)("#install").on("input blur", "input", () => {
				this.validateInputs();
			});
			(0, jquery.default)("#install").on("submit", async (event) => {
				event.preventDefault();
				this.validateInputs();
				const isUsernameValid = this.checkInput((0, jquery.default)("#admin"), lang["install"]["username_empty"]);
				const isPasswordValid = this.checkInput((0, jquery.default)("#admin_psw"), lang["install"]["password_rule"]);
				const isPasswordConfirmValid = this.checkPasswordConfirmation((0, jquery.default)("#admin_psw"), (0, jquery.default)("#admin_psw_confirm"), lang["install"]["repassword_error"]);
				if (!isUsernameValid || !isPasswordValid || !isPasswordConfirmValid) return false;
				if (!(0, jquery.default)(event.target).find("#submit").prop("disabled")) {
					const formData = new FormData(event.target);
					const formObject = {};
					for (let [key, value] of formData.entries()) formObject[key] = value;
					await this.showSwal({
						text: lang["install"]["installing"],
						showConfirmButton: false,
						allowOutsideClick: false,
						allowEscapeKey: false,
						willOpen: () => {
							(0, jquery.default)("#install, #form-title").hide();
						},
						didOpen: () => {
							this.showSwal({
								text: lang["install"]["installing"],
								allowOutsideClick: false,
								allowEscapeKey: false,
								willOpen: async () => {
									(0, jquery.default)("#install, #form-title").hide();
									formObject["request"] = "start_install";
									await this.sendFormData("api.php", formObject, "POST", function(res) {
										sweetalert2.default.hideLoading();
										if (res["status"] === true) sweetalert2.default.fire({
											icon: "success",
											html: lang["install"]["install_success"] + "<br>" + lang["install"]["install_remove"]
										}).then((result) => {
											if (result.isConfirmed) window.location.href = InstallHelper.backURL;
										});
										else sweetalert2.default.fire({
											icon: "error",
											html: res["message"]
										}).then((result) => {
											if (result.isConfirmed) window.location.reload();
										});
									}, async (error) => {
										await this.showSwal({
											title: "Error",
											html: error,
											icon: "error"
										});
									});
								}
							});
						},
						didClose: () => {
							(0, jquery.default)("#install, #form-title").show();
						}
					});
				}
			});
		}
	};

//#endregion
return InstallHelper;
});